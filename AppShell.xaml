<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:P2PChat"
       x:Class="P2PChat.AppShell">
    <ShellContent Title="Главная" ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage" />
    <ShellContent Title="Чат" ContentTemplate="{DataTemplate local:ChatPage}" Route="ChatPage" />
</Shell>